
function QueryDate(_days) {
    this.days = _days; // mocked value
}

QueryDate.prototype.daysToJourney = function (_journey) {
    _journey // assume we use it
    return this.days;
};

module.exports = QueryDate;